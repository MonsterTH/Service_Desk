$kind: http-request
name: 'Get Category By ID'
method: GET
url: '{{api_base_url}}/categories/{{category_id}}'
order: 3000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
