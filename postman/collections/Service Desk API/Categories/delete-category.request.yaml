$kind: http-request
name: 'Delete Category'
method: DELETE
url: '{{api_base_url}}/categories/{{category_id}}'
order: 6000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
